<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>The Learning Trail Catalog</title>
  <link rel="icon" href="/static/learning-trail-icon.png">
  <link rel="stylesheet" href="/static/global.css">
  <link rel="stylesheet" href="/static/search.css">
</head>
<body>
  <header class="header">
    <div class="header-content">
        <img src="/static/learning-trail-full.png" width="100" alt="Learning Trail Logo">
      <a href="/" class="btn home">Home</a>
      <form action="{{ url_for('search') }}" method="get" class="search-group">
        <select name='search_crit' id='search_crit' class='search_crit'>
          <option value='title'>Title</option>
          <option value='author'>Author</option>
          <option value='series'>Series</option>
          <option value='genre'>Genre</option>
        </select>
        <input type="text" name="q" class="search-input" placeholder="Search catalog…" autofocus>
        <button type="submit" class="btn search-btn"> Search </button>
    </form>
    </div>
  </header>
  <main class="container">
    <h1 class="title">Search Results{% if query %} for “{{ query }}”{% endif %}</h1>

    {% if results %}
      <ul class="search-results">
        {% for book in results %}
          <li class="result-item">
            <a href="{{ url_for('bookPage_get', title=book.title) }}" class="book-title">
              {{ book.title }}
            </a>
            <div class="book-details">
              <span class="author">by {{ book.author }}</span>
              {% if book.series %}
                <span class="series">| Series: {{ book.series }}</span>
              {% endif %}
            </div>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p>No books found matching your search.</p>
    {% endif %}
  </main>

</body>
</html>
