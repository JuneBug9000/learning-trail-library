<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>The Learning Trail Catalog</title>
  <link rel="icon" href="/static/learning-trail-icon.png">
  <link rel="stylesheet" href="/static/global.css">
  <link rel="stylesheet" href="/static/register.css">
</head>
<body>
  <header class="header">
    <div class="header-content">
        <img src="/static/learning-trail-full.png" width="100" alt="Learning Trail Logo">
      <a href="/" class="btn home">Home</a>
      <form action="{{ url_for('search') }}" method="get" class="search-group">
        <select name='search_crit' id='search_crit' class='search_crit'>
          <option value='title'>Title</option>
          <option value='author'>Author</option>
          <option value='series'>Series</option>
          <option value='genre'>Genre</option>
        </select>
        <input type="text" name="q" class="search-input" placeholder="Search catalog…" autofocus>
        <button type="submit" class="btn search-btn"> Search </button>
      </form>
    </div>
  </header>

  <main class="container">
    <h1 class="title">Register New Book</h1>

    {# flash messages #}
    {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
        <ul class="flashes">
          {% for category, message in messages %}
            <p class="flash {{ category }}">{{ message }}</p>
          {% endfor %}
        </ul>
      {% endif %}
    {% endwith %}
    <p> For fields that don't apply, fill out with "N/A". If the book is not part of a series, leave Series Number blank. </p>

    <form method="POST" action="{{ url_for('register') }}" class="register-form">
      <label for="title">Title</label>
      <input id="title" name="title" type="text" required>

      <label for="author">Author</label>
      <input id="author" name="author" type="text" required>

      <label for="series">Series</label>
      <input id="series" name="series" type="text" required>

      <label for="series_number">Series Number</label>
      <input id="series_number" name="series_number" type="number" min="1">

      <label for="genre">Genre</label>
      <input id="genre" name="genre" type="text" required>

      <label for="location">Location</label>
      <input id="location" name="location" type="text" required>

      <label for="copies">Number of Copies</label>
      <input id="copies" name="copies" type="number" min="1" value="1" required>

      <button type="submit" class="btn checkout">Register Book</button>
    </form>
  </main>
</body>
</html>